/*****************************************************************************
    Offline Status
*****************************************************************************/
I decided not to change Iterate aside from a minor use of unWrap. A when not broke dont fix decision. I did change where useDefaultSpace gets reset so it happens in opPrint and opString; that is: useDefaultSpace is true by default and can be toggled within a print statement via the $ shortcut. I simplified genParseTest in the parser incantation (that means the parser incant file); it is now shorter and conforms to the design decision that a rule can have data, attributes, or members: pick one. Loud error if a rule does not comply. I changed grammar so that rules like Braced pick up labels on literal attributes like "["; so Braced rule now looks like:
        Braced      leftBrace="["- ExpressioN rightBrace="]"-;
    That is a step to get what genParseTest for Braced to look like:
    code={
        if  leftBrace() AND ExpressioN() AND rightBrace(); return runRuleAction(this);
        }
    Does not mean the Braced parse action will be able to find leftBrace and ExpressioN ... That is likely to require a change to parseRule to insert those as local fields. Will worry about that when ready to test parseRule. Not there yet.
Files I changed:
    RuleStuff.twk changed constructors to look for parentStuff and set it if the rule has a parent w/rStuff
    Generate.rtn no changes made that show up in running code. WIP.
    GrouActions.rtn and Instruct.rtn changed where useDefaultSpace is set/reset
    ruleActions.rtn changed how aCTionDefinE sets rStuff for rule attributes
Ran POP in xcode and things work. Full POP warranted but probably can wait until kitchen cleaning time.
On the task list:
    Would like to push ahead on cleaning up comments -> DesignDocs. That looks like a minion task to me.
    Would like to work on getting jitting to work alongside interpreter not mixed in with it. Not sure where that sits. Design work may be needed. I think Clod looked into it.
