/*****************************************************************************
    Offline Status
*****************************************************************************/

/*****************************************************************************
    Working on
*****************************************************************************/
The mechanism settles it better than any counter could, and my recommendation is the zero-branch: full retirement. The command escape hatch answered "kant needs a way to find it" — but the measurement's real finding is that nothing needs to find it. GroupMain builds NamE and NumbeR with tokened = true directly and no tokenize term; captureSpan at GroupItem.twk:1142 is the live consumer; the comments name the succession in their own words. Relocating a corpse to a nicer register keeps the corpse. Your memory was right, and the code had already written it down.

On Clod's stated gap — no known-positive control, so the counter can't exclude "probe on a function nothing calls by that name": the deletion is the positive control, run in the only direction available. A probe that can't be made to fire can't certify death, but a removal that leaves the fleet, both baselines, and the tokenize-heavy fixture byte-identical certifies it as hard as this project certifies anything — and unlike the counter, a hidden caller shows up loud, as a red row or a resolution failure naming the site, with the one-commit revert floor underneath. H11 is satisfied by the stronger instrument, not waived.

Scope lines for the sweep, so it's one clean commit:

Goes: the method (GroupActions.rtn:1772), the extern (and its groups.ext line — support repo committed, per the standing rule), the GroupMain construction and binding at :172-176, the inert tokenize^@; grammar mirror, and the tokenize term from any unparked rule still spelling it.
HeX: it's parked and its parking note already says it removes a read-through. Strip the term inside the comment block anyway, with a one-line note — otherwise HeX un-parks someday and dies resolving a name that no longer exists, three screens from the reason.
Stays, explicitly: tokened, processFlags' setting of it, and captureSpan — the live road. The pre-registration for the commit is "fleet, oneTest, jsonTest, parseClass census all byte-identical," and any motion is a caller the counter missed, which would be a finding, not a failure.
Ledger: hookRules.md row one gets its obituary — retired by measurement plus mechanism, F-37 closed on the zero branch, command branch recorded as considered-and-unneeded. F-31's strike is already banked.

The rule census drops by one and the parse-generation population shrinks by its strangest member — the snake finally leaves the room by the door instead of eating its tail.

/*****************************************************************************
    Clod response
*****************************************************************************/
