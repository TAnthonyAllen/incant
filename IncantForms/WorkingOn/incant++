/*****************************************************************************
    Offline Status
*****************************************************************************/
I revised countInputInTmp and it looks okay to me, however, I am not sure why it is in fixits so maybe I am missing something
I looked into dataCrash and rather than change it I revised parser to walk/generate parse and compile DatA and all its components. Works fine altho I had to change the e label to exponent in the PoweR rule (in GroupMain).
I think both countInputInTmp and dataCrash incantations in fixits can be retired, altho Clod will have to review them first.
I modified GroupMain to add labels to literal rules like [ which now has a new rule leftBrace='['; Same with other literal rules defined in GroupMain which now contains the following code and corresponding changes to rules defined therein that use those that were literals
   /**************************************************************************
        initialize punctuation fields
    *************************************************************************/
    strap           = grok += new("leftBrace");
    text            = "[";
    strap           = grok += new("leftCurly");
    text            = "{";
    strap           = grok += new("leftParen");
    text            = "(";
    strap           = grok += new("rightBrace");
    text            = "]";
    strap           = grok += new("rightCurly");
    text            = "}";
    strap           = grok += new("rightParen");
    text            = ")";

/*****************************************************************************
    Working on
*****************************************************************************/
/*****************************************************************************
    Clod response
*****************************************************************************/
