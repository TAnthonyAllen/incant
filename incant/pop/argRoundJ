Start();
include(unitTests);
include(generate);
include(utilities);
search reset stack Grokking;
search Generating UnitTests bcOPs Utilities list;

register(ArgRound);
define
    arDepth=0;
    arSrc=7;
    arOther=41;
    arA code={
        ++arDepth;
        arMine = arDepth;
        if arDepth < 3;     arB(arOther);
        print "  A depth" arMine " sees argument =" argument " local =" arMine:;
        };
    arB code={
        arA(arSrc);
        };
    arRun code={
        arA(arSrc);
        };
    ;
testing(arRun);
print "ARGROUNDJ SENTINEL -- reached the foot":;
stop();

argRoundJ -- argRoundT's JIT arm, IN ITS OWN FILE.

Two files because a fixture gets ONE testing(): a second call on the same
action hits the sequential-state-corruption tar baby, and bear-trap #25 says
an interpreted run first consumes isCoded and silently reroutes testing() to
jitRunIfTest. So the interpreted oracle lives in argRoundT and the jitted arm
lives here, and neither can spoil the other.

⚠ IT WAS PINNED TO A KNOWN DEFECT FOR ONE DAY AND IS NOW GREEN. jitArgBake:
where the JIT gave a callee its OWN FUNCTION rather than inlining it, the
callee's `argument` was read from an emit-time BAKED address and did not track
the run-time bind, so this file printed ADDRESSES where argRoundT's interpreted
column printed 7.

DISCHARGED 2026-09-05, and not by the machinery anyone proposed building. It
fell out of item 3's flag hoist: setting isArgument ABOVE runAction's jitting
gate means the JIT frame prologue's (isLocal || isArgument) walk finally SEES
the argument and gives it an alloca, so the read is a FRAME LOAD fed per
activation instead of a baked address. Confirmed on the emitted IR -- jit_jabA
gained `%argument = alloca i32` with its `%prolog` load, where before it had
only the local's.

    interpreted (argRoundT)   argument = 7 at every depth
    jitted      (this file)   argument = 7 at every depth   AGREE

⚠ THE LOCAL COLUMN AGREES ON BOTH ENGINES TOO -- 3/2/1 -- and it is still a
separate assertion rather than a redundant one. While the argument column was
wrong, the local column agreeing is what said the defect was the ARGUMENT
CHANNEL and not the frame. That attribution is why the fix could be aimed.

⚠ IT STAYS A SEPARATE FILE FROM argRoundT, and not out of history: a fixture
gets ONE testing(). A second call on the same action hits the sequential-state
tar baby, and an interpreted run first consumes isCoded and silently reroutes
testing() to jitRunIfTest (bear-trap #25). Two files means neither arm can
spoil the other.
