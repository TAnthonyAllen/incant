Start();
include(unitTests);
include(generate);
include(utilities);

search reset stack Grokking;
search Generating UnitTests bcOPs Utilities list;

register(FixIterT1m);
define
    leafA   i j;
    leafB   k l;
    trunk   leafA leafB;
    fixIterT1mHere=0;
    fxRows=0;
    fxInner=0;
    fxLeaves=0;

    walkA argument code={
        fxRows = fxRows + 1;
        print "A " taG argument:;
        if argument.listLengtH;     fxInner = fxInner + 1;
        iterate grup on argument;
        while ++grup
            walkB(grup);
        };

    walkB argument code={
        fxRows = fxRows + 1;
        print "B " taG argument:;
        if argument.listLengtH;     fxInner = fxInner + 1;
        iterate grup on argument;
        while ++grup
            walkA(grup);
        };

    fxRun code={
        cerr "== fixit iterT1m -- the walk is RULED, the announcement is not ==":;
        cerr "-- the walk as it runs TODAY (each line is one visit) --":;
        walkA(trunk);
        cerr :;
        cerr "ROW 1  the walk itself -- CLOSED 2026-08-20, Tony read it and blessed it":;
        cerr "   pinned   7 visits, every node exactly once":;
        cerr "            genLadder/iterT1m.target, green in pop.sh":;
        cerr "   measured " fxRows " visits":;
        fxLeaves = fxRows - fxInner;
        cerr "ROW 2  the refusal announcement -- STILL OPEN, and this is the ask":;
        cerr "   visits " fxRows " of which " fxInner " have a list":;
        cerr "   so " fxLeaves " leaf visits, one REFUSED iterate each":;
        cerr "   so the announcement, if it existed, would print that many times":;
        cerr "   measured 0 lines of aCTionIterate: source on stderr":;
        cerr "   because the cerr that printed it was DELETED in 9c4962b, 08-15":;
        cerr :;
        cerr "   ONE DECISION: restore the cerr and pin 4, or retire the row and":;
        cerr "   say what covers the poison instead. It is NOT pinned to 0.":;
        fixIterT1mHere = 0;
        cerr "ANCHOR fixIterT1mHere -- both numbers are on screen above":;
        cerr "FIXIT iterT1m SENTINEL -- reached the end":;
        stop();
        };
    ;
fxRun();

============================================================================
THE MECHANISM

incant/iterT1m walks a small tree with two mutually recursive iterators, walkA
and walkB, each iterating the node the other handed it. The fleet pins two
things about it: the trace of the walk, and how many times a refused iterate
announces itself.

ROW 1 -- THE WALK. CLOSED 2026-08-20.

It was pinned as a KNOWN WRONG answer: fourteen visits, every leaf seen twice,
because mutual recursion lost its locals. It now visits every node exactly
once. Tony ran this file, read the walk, and blessed it, so the pin graduated
from genLadder/iterT1m.divergence to genLadder/iterT1m.target and pop.sh went
from fifty one green to fifty two.

The sentence behind the re-pin is a subsequence claim rather than a count. The
old fourteen line pin differed from today's output by DELETIONS ONLY -- nothing
added, nothing reordered. Today's walk IS the old walk with its seven duplicate
visits removed, and it is the exact seven line trace incant/iterT1m's own header
pre-registered as correct, in order. The fixture named the right answer before
the world produced it.

WHAT IS DELIBERATELY NOT CLAIMED

That header goes on to reason that seven in that order means the recursion
inference covers mutual recursion after all. It does not follow. field.recursive
is still set by identity against currentMETHOD at ruleActions.rtn 1320,
unchanged, so it still covers direct self reference only and neither walkA nor
walkB names itself. The walk is right for some other reason and nobody has
measured which. The target pins the ANSWER, which has been read. It does not
pin a mechanism nobody has measured.

ROW 2 -- THE ANNOUNCEMENT. STILL OPEN, AND THIS IS WHAT IS OWED.

KE-4 held this red pending a cause and named three candidates: the announcement
deleted, the poison not taking, or mutual recursion silently starting to work.
The cause is now established and it is the FIRST of the three, not the third.
git log dash S on the string aCTionIterate colon source puts the removal in
9c4962b, 2026-08-15, Tony's own offline kant work. The line

    cerr aCTionIterate colon source, tag, has no list

was deleted from aCTionIterate's refusal arm in ruleActions.rtn. The arm is
otherwise intact, so the poison still takes and the walk still terminates at
exit 0. There is simply nothing left to count.

SO THE TWO ROWS DID NOT MOVE FOR THE SAME REASON, which is what the first
version of this file asserted and it was wrong. The walk went fourteen to seven
because mutual recursion started working, on or before 2026-08-13. The count
went seven to four with it, and then four to zero on 08-15 when the cerr went.

AND IT IS NOT RE-PINNED TO ZERO, DELIBERATELY

Pinning zero here would be an absence assertion, which rule H4 exists to
forbid. It would go green forever, and it would go green hardest on the day
somebody deleted the poison as well. The row stays red with a named cause until
ruled. One line either way: restore the cerr and pin the leaf count this file
prints, which restores presence with value, or retire the row and say what
covers the poison instead.

Footnote: pop.sh row iterT1m announces its refusal, docs/knownErrors.md KE-4.
