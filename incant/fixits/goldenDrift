Start();
include(unitTests);
include(generate);
include(utilities);
search reset stack Grokking;
search Generating UnitTests bcOPs Utilities list;

register(GoldDrift);
define
    gdRun code={
        print "=== CLUSTER 1 -- a value that comes back as its own tag ===":;
        print "  PINNED    lefty: got reset to 46":;
        print "  PINNED    printTest: lefty should return itself. It returns 46":;
        print "  MEASURED:":;
        printTest();
        print "=== CLUSTER 2 -- an OR that takes the other arm ===":;
        print "  PINNED    testOR: Saw 13 having a few doubts ends here":;
        print "  MEASURED:":;
        testOR();
        print "=== THE MECHANISM, three arms and an anti-vacuity control ===":;
        gdA = gdSelf();
        print "  A  body references its OWN name    " gdA " (want 46)":;
        gdB = gdNoSelf();
        print "  B  body references ANOTHER field   " gdB " (want 46)":;
        gdC = gdPlain();
        print "  C  body references no field at all " gdC " (want 46 -- anti-vacuity)":;
        fixGoldenDriftHere = 0;
        print "ANCHOR fixGoldenDriftHere -- PINNED and MEASURED sit together above":;
        };
    gdSelf=3   code={ gdSelf += 43; return gdSelf; };
    gdOther=3;
    gdNoSelf   code={ gdOther += 43; return gdOther; };
    gdPlain=3  code={ return 46; };
    ;
gdRun();
print "GOLDENDRIFT SENTINEL -- reached the foot":;
stop();

LANE:  parser
BLAST: OVERLAPS

goldenDrift -- SIX VALUES WENT MISSING FROM incant/baselineTests AND THE FLEET
STRUCTURALLY COULD NOT SEE IT. Minted 2026-08-30.

⚠⚠ STATUS 2026-09-01 (SEQ 103 Part 1, Tony's verdict): FOUR OF THE SIX ARE
DISCHARGED. TWO REMAIN, PARKED. The count in the title above is the mint-day
count and is kept so the arithmetic below still reads; the live tally is here.

  cluster                 was              is now
  1  lefty x2             BROKEN           ✅ DISCHARGED -- BEHAVIOUR
  2  testOR               BROKEN           ✅ DISCHARGED -- NO DEFECT, GOLDEN STALE
  3  second / third       BROKEN           PARKED, unattributed
  4  width / text         BROKEN           PARKED, unattributed

CLUSTER 1 DISCHARGED BY MEASUREMENT, NOT BY RE-PIN, AND THAT IS THE STRONGER
OUTCOME. The 08-30 frameSTAK remedy (second channel for the frame save-stack,
GroupActions.rtn) took. Measured 2026-09-01 on this file's own arms:

    A  body references its OWN name     46   (want 46)
    B  body references ANOTHER field    46   (want 46)
    C  body references no field at all  46   (want 46 -- anti-vacuity)

and both cluster-1 rows now MATCH the golden as it already stood -- the golden
was never wrong about lefty, so NOTHING WAS RE-PINNED for cluster 1. The row
simply left the diff. Arm C is the anti-vacuity control and holding at 46 is
what says the walk still ran.

CLUSTER 2 RE-PINNED, AND HERE IS THE SENTENCE CLAUSE 2 DEMANDS. Tony
hand-measured printTest() and testOR() on 2026-09-01 and ruled both CORRECT AS
DESIGNED: the drift is in the golden, not in the function. testOR is
`if !righty OR feeling;` with `righty=13` and
`feeling='having a few doubts ends here'` both declared (incant/scopeUnits:100,
:140). `!` binds tight to its operand, so the test is `(!righty) OR feeling` --
false OR a live field -- which is TRUE, and the true arm prints
`Saw 13 having a few doubts ends here`. THAT IS THE DESIGNED ANSWER. The pin
that said `I see nothing` required the OR to be FALSE.

  ⚠ THE PIN WAS ELEVEN WEEKS STALE. `git log -S` puts its last movement at
    1597282, 2026-06-14. It is the oldest thing in this file by a wide margin.

  ⚠ AND THE MECHANISM -- WHY IT WAS FALSE IN JUNE -- IS A GRADED CANDIDATE AND
    NOT A FINDING, because nobody has run it. `feeling` and `testOR` have both
    existed unchanged since 2026-05-31 and moved together into scopeUnits at
    b9fbb5d (2026-08-01, "name-scope recon"), so the likeliest story is that
    `feeling` DID NOT RESOLVE inside testOR's body in June and the name-scope
    work made it resolve. PLAUSIBLE, UNMEASURED, and it is recorded at that
    grade deliberately -- this project's ledger says causal claims here fail
    about half the time, and the re-pin does not depend on this one. The
    re-pin rests on Tony's ruling plus the operand reading above, both of
    which are checkable today.

⚠⚠ AND A DEFECT IN THIS FILE'S OWN TABLE, FOUND WHILE DISCHARGING THE ABOVE:
CLUSTER 4's `text` ROW IS PRINTED BACKWARDS. The mint-day table below says
pinned `text=one`, measured `text`. The tree says the opposite -- the GOLDEN
carries `stuff text pad=6;` and the MACHINE produces `stuff text=one pad=6;`.
So for that half of cluster 4 the value is PRESENT now and ABSENT in the pin,
which is the reverse of the disappearing-value story the whole file is built
on. `width=50%` -> `width=50` is still a genuine disappearance. NOT CHASED --
cluster 4 is parked by ruling -- but whoever un-parks it must re-read the row
before reasoning on it, because a mis-transcribed table that generates
well-reasoned unnecessary questions is exactly the failure this project has a
named ledger for.

⚠ CLAUSE 3 IS STILL OWED AND IS NOW MORE URGENT, NOT LESS. The golden is a
MIXED artifact from today: line 16 is current, lines 65-66 and 70-71 are
DELIBERATELY STALE. Nothing in the fleet reads any of it -- pop.sh asserts only
exit 0 and the presence of the last line -- so if the content diff of clause 3
is ever switched on it will come up RED ON TWO CLUSTERS BY DESIGN, and that is
correct rather than a regression. A note to this effect now sits beside the
baselineTests block in genLadder/pop.sh.

THE MINT-DAY RECORD FOLLOWS, UNCHANGED EXCEPT WHERE MARKED.

THE MECHANISM IS BROKEN. THE INSTRUMENT THAT SHOULD HAVE SAID SO ASSERTS THE
WRONG THING. Both halves are owed and the order they are fixed in matters.

WHAT IS ACTUALLY WRONG

`incant/baselineTests.golden` and what the binary produces disagree on six
values in four clusters. Every one of them is a value DISAPPEARING:

  cluster            pinned                    measured now
  lefty (x2)         46                        leftydata type has no toString()
  testOR             I see nothing             Saw 13 having a few doubts
  testForC attrs     second=56;  third=WTF?    second;  third
  nested form        width=50%   text=one      width=50   text

THIS IS A REGRESSION AND NOT A STALE PIN, AND ONE COMMAND PROVES IT. The
fixture's SOURCE still declares every missing value -- unitTests:147 has
`lefty=3 code={ lefty += 43; ... }`, :153 has `second=56 third="WTF?"`, :102
has `width=50%`, :103 has `text=one`. The whole diff of incant/unitTests since
the golden was last pinned is ONE line, `aGroup one two three` gaining a `=2`,
which touches none of them. And the fixture's own prose at :56 and :68 states
the expected answers in words: "lefty should return itself. It returns 46".
So the declarations stand, the pin stands, and the machine changed underneath
both.

WHY NOBODY SAW IT, WHICH IS THE HALF WORTH KEEPING

`genLadder/pop.sh` asserts baselineTests on TWO things: that it exits 0, and
that its last line is present. It never diffs the content. So a 76-line pinned
artifact sits in the tree with nothing reading it, and six values could vanish
without moving a single fleet row. The green was live evidence about truncation
and evidence of NOTHING about the values.

⚠ IT WAS FOUND BY ACCIDENT, which is the H12 lesson arriving in a file instead
of a session. The `*` facility predicted this fixture would go red on `down=*`;
it did not, and checking WHY the green was trustworthy is what opened the file.
Nobody was auditing it. Nobody had reason to.

⚠ ATTRIBUTED 2026-08-30 BY BISECT, AND THE 17d3642 LEAD IS FALSIFIED. The
earlier version of this file named 17d3642 (08-04) as the candidate. It is not
the cause of any cluster. That is why clause 1 exists and why a lead is not an
attribution.

⚠⚠ AND THE DRIFT IS AT LEAST TWO INDEPENDENT CAUSES IN TWO DIFFERENT ERAS. A
single-probe bisect finds ONE cause and looks complete, which is bear-trap #19's
corollary wearing a bisect: the search space was one cluster.

  CLUSTER 1  lefty x2      ATTRIBUTED  168453d, 2026-08-10, RUNG B
  CLUSTER 3  second/third  NARROWED    2026-08-02, the finalize-phase day,
                                       window 03d5c73 (good) .. 778c140 (bad),
                                       eight commits, FOUR unbuildable and
                                       skipped by the bisect
  CLUSTER 2  testOR        NOT BISECTED
  CLUSTER 4  width / text  NOT BISECTED

CLUSTER 1's ATTRIBUTION, MEASURED THREE WAYS.

168453d is "CLAIM KANT-8 REPAIRED: value-capture seam + unconditional bracket
(SEQ 27, both rungs)" -- the K5/K6 work itself. Two one-arm reverts separate the
rungs:

  capture disabled, bracket ungated (rung B alone)   lefty STILL WRONG
  four `if field.recursive` gates restored (rung A)  lefty = 46

So it is RUNG B, the unconditional frame bracket, and not the value-capture seam.

THE MECHANISM, and rows A/B/C above measure it live:

  A  a field whose code body references ITS OWN NAME       BROKEN
  B  a field whose code body references ANOTHER field      fine
  C  a field whose code body references no field at all    fine

Only the self-mentioning class breaks. An undeclared name in an action body is
minted as an action LOCAL (bear-trap #39), `saveLocalFields` BLANKS non-argument
locals on entry, and rung B made that blanking universal. So `gdSelf += 43`
pre-loads onto a blanked node and comes back as its own tag -- bear-trap #26
row 1, exactly, which is why the printed answer is a NAME followed by a
toString failure rather than a wrong number.

⚠ THE IRONY IS THE FINDING, NOT A GARNISH. `recursive` is set at PARSE time BY
IDENTITY -- that is, precisely when a body mentions its own name. So the OLD
gate ran the bracket on EXACTLY the self-mentioning actions, and rung B's own
commit message says the coupling out loud: "while the return seam handed back
the local's own node, ungating the sweep blanked every returned local rather
than only the self-mentioning ones." Rung A was the stated prerequisite that
made ungating safe. IT DOES NOT COVER THIS DOOR: the capture lives in runAction,
and a field invoked BY REFERENCE with a code body never reaches it -- which is
why disabling the capture changes nothing here.

NEXT: REMEDY -- ASSERTABILITY AND BEHAVIOUR, IN THAT ORDER, AND THE ORDER IS
NOT NEGOTIABLE

Three clauses. CLAUSE 2 BEFORE CLAUSE 1 IS PROHIBITED: re-pinning first banks
six unknown breakages as normal, permanently, and no later reader can tell the
difference.

  1  ATTRIBUTE THE DRIFT.  ✅ DONE for cluster 1 (168453d rung B, measured
     three ways).  STILL OWED for clusters 2 and 4, and cluster 3 needs the
     four unbuildable commits of 2026-08-02 reconstructed by retok before a
     bisect can pass through them.  BEHAVIOUR.

  ⚠⚠ CLUSTER 1 IS REMEDY AS OF 2026-08-30, AND THE FIX WAS NOT WHERE THIS FILE
     SAID IT WAS.  BEHAVIOUR.  Site: GroupActions.rtn, saveLocalFields /
     restoreLocalFields, via the new frameStak/frameFind pair.

     THE DIAGNOSIS ABOVE NAMED THE BLANKING WALK.  IT WAS WRONG, and the probe
     that corrected it is worth more than the fix: saveLocalFields NEVER FIRES
     for a self-mentioning field at all.  What broke the value was its FIRST
     LINE -- `action.stak = recurseSTAK` -- which writes the action node's DATA
     SLOT.  Measured directly:

         SLFENTRY action=spSelf data=5  text=3        <- the field holds 3
         SLFAFTER action=spSelf data=12 text=spSelf   <- 12 = isSTAK, value gone

     ONE CHANNEL, ONE MEANING: the node's data slot carried the FIELD'S VALUE
     and the FRAME'S SAVE-STACK.  The cure is the standing one -- a second
     channel.  The stack now lives on a noPrint CHILD, `frameSTAK`.

     TAKEN vs NOT TAKEN, on the mechanism rows this file prints:
         taken      A 46   B 46   C 46
         not taken  A <a NAME plus a toString complaint>   B 46   C 46
     Only arm A moves, which is what says the repair is scoped to the class and
     did not reach for anything else.

     RUNG B STANDS WHOLE and the recursive gates STAY SHUT.  This narrows the
     bracket's scope to what it was always for; it does not restore the gate,
     whose two diseases (parse-time identity, run-time clearing) are unchanged.
  2  RE-PIN ONLY WITH THE SENTENCE.  A target that moved is a claim that the
     world changed and the claim needs a cause -- one named per cluster, or the
     cluster is not re-pinned.  BEHAVIOUR.
  3  GIVE THE ROW A CONTENT DIFF in pop.sh, so the golden pins the golden
     instead of pinning the fact that the process reached its last line.
     ASSERTABILITY -- the machine will not move and the fleet count will,
     which is the expected and correct shape of an assertability remedy.

⚠ WHY EVERY LINE HERE IS `print` AND NOT `cerr`, WHICH IS NOT A STYLE CHOICE.
The actions this file fires -- printTest, testOR -- report on STDOUT, because
that is what the fixture they live in does. Labelling them from `cerr` puts the
PINNED value and the MEASURED value on two different streams, and stdout is
block-buffered when it is not a tty, so the two halves of every comparison
arrive out of order or in separate blocks. The whole point of this citizen is
that the two numbers sit side by side on screen. One stream, or no comparison.

⚠ THE OLD BEST GUESS IS PROMOTED IN HALF AND CORRECTED IN HALF, and the
correction is the useful part. It named `+=`'s pre-load, and the pre-load IS
where the wrong value is produced -- rows A/B/C confirm it. But it guessed the
PRE-LOAD GUARD had changed. It had not. What changed is that the field the
pre-load reads is now BLANKED BEFORE IT, by a bracket that used to be gated.
The symptom was at the right line; the cause was one stroke upstream.

⚠ CLUSTER 1's FIX IS NOT "RESTORE THE GATES". Rung B is architecturally right
and its reasons stand: the gate is set at parse time BY IDENTITY so mutual
recursion never sets it, and it is CLEARED at run time so behaviour depends on
invocation history. Restoring it re-opens both. The fix is to extend rung A's
capture to the door this class arrives on, or to stop saveLocalFields blanking
a local that shadows a defined field. THAT CHOICE IS A RULING, NOT A
MEASUREMENT.
