Start();
include(unitTests);
include(generate);
include(utilities);
search reset stack Grokking;
search Generating UnitTests bcOPs Utilities list;

register(JitArgBake);
define
    jabSrc=7;
    jabOther=41;
    jabDepth=0;
    jabAnchor;
    jabA argument code={
        ++jabDepth;
        jabMine = jabDepth;
        if jabDepth < 2;    jabB(jabOther);
        print "   argument =" argument "   local =" jabMine:;
        };
    jabB argument code={ jabA(jabSrc); };
    jabRun code={ jabA(jabSrc); };
    ;

print "JITTED      -- want `argument = 7` on both lines:":;
testing(jabRun);
jabDepth = 0;
print "INTERPRETED -- the oracle, and it is right:":;
jabRun();
jabAnchor = 0;
cerr "fixJitArgBakeHere -- the two blocks above are the whole issue. Jitted argument column: 7 means fixed, a large number is the baked address":;
print "JITARGBAKE SENTINEL":;
stop();

LANE:  jit
BLAST: DISJOINT

NAME      jitArgBake -- an own-function JIT callee reads `argument` at the
          emit-time BAKED address, not the run-time bound slot.

WHAT      Where the JIT gives a callee its own function instead of inlining it,
          the callee's `argument` read is a load from an address baked when the
          body was compiled. The run-time bind (jitBindArgRT) writes somewhere
          else, so the emitted body reads whatever was bound at COMPILE time --
          and prints an address where the interpreter prints 7. Silent: degrade
          count 0, exit 0. Inlined callees are CORRECT; this is only the
          own-function road.

WHERE     jitEmitters.rtn -- jitBindArgRT binds at run time; jitEmitBareRead
          and jitSeedField bake at emit time. The two never meet.

WHY NOW   B landed 2026-09-05 and fixed the inlined road on both engines. This
          is what B did not reach. PRE-EXISTING, not caused by B -- the same
          read answered 0 before B.

WHAT YOU JUST RAN. Two blocks, same action, same input, two engines. The
interpreted block is the oracle and prints 7 twice. The jitted block prints an
address twice. The LOCAL column agrees on both engines, which is what says the
defect is the argument channel and not the frame.

THE DISCRIMINATOR IS ONE LINE IN THE RUN: `callee jabA built as jit_jabA`
means own-function, and therefore in scope. Its absence means the callee
inlined and is already correct -- minionWork/probeArgJit is that case and reads
8 / 22 / oracle 14 at degrade 0.

NEXT: REMEDY -- BEHAVIOUR. Discharged 2026-09-05, same day it was minted.

THE FIX WAS NOT A BUILD ON THIS ROAD AT ALL. It fell out of item 3's runAction
revision: isArgument is now stamped ABOVE the jitting gate, so the JIT frame
prologue's (isLocal || isArgument) walk finally SEES the argument and gives it
an alloca. The read became a FRAME LOAD, fed per activation by the caller's
bind, instead of a baked absolute address.

SITE: GroupActions.rtn, runAction -- the mint and `ruleArg.isArgument = true`
sit above `if jitting`. Nothing was added to jitSeedField or jitEmitBareRead.

TAKEN vs NOT, on this file's own output:
    NOT taken   jitted block prints large numbers, interpreted prints 7
    TAKEN       both blocks print 7, at every depth

ON THE EMITTED IR, which is the stronger witness because it names the mechanism
rather than the answer:
    NOT taken   define jit_jabA has an alloca for the LOCAL only
    TAKEN       it also has `%argument = alloca i32` with its `%prolog` load

⚠ THE BEST GUESS WAS RIGHT ABOUT THE SHAPE AND WRONG ABOUT THE WORK. "Feed a
slot, stop baking" is exactly what happened. The promoting measurement it named
-- does the own-function road have a frame slot for the argument at all --
answered NO before the hoist and YES after, and no new machinery was needed
because the prologue already re-loads its slots per activation.

THE FOLDED-IN HALF IS ALSO DONE. jitBindArgRT's refusal arm and its bind-by-body
fallback are removed, matching runAction: the tripwire guarded a union that
could hold foreign data because the attribute was USER-DECLARED, and runAction
now MINTS the slot, so the union is ours by construction. The arm had refused,
said so, and then done the bind-by-body anyway -- the exact road SEQ 131 removed
from runAction. Behaviour-neutral by measurement (fleet unmoved), because it
never fired. THE PENDING SLOT STAYS: it is the emitted path's channel bracket,
paired with jitSaveFrameRT/jitRestoreFrameRT, and was not what was wrong.

FLEET COVER, so this citizen is not the only thing holding it:
    incant/argRoundJ   re-pinned red -> 7 the same day, with its cause
    incant/argJitT     the inlined road, promoted after it regressed silently
Both are in genLadder/pop.sh.

⚠ RETIREMENT IS TONY'S OR CLAY'S RULING, NOT CLOD'S (rule F2). This file is now
a VERIFICATION HARNESS in Addendum 3's sense: run it, and the two blocks should
agree at 7. It is left in the queue for that ruling rather than retired.
