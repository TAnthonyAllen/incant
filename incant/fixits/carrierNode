LANE:  parser
BLAST: OVERLAPS

NAME      carrierNode -- parser(Start) receives a carrier, not Start.
SEVERITY  BLOCKER. It is the campaign gate. The flip cannot certify while the
          acceptance line fails, and this is why it fails. Under F1 it is the
          only citizen left standing.
SEE       incant/acceptStartT (parked, un-parks at the flip) - incant/roundTripT
          - incant/broadcastT - kant8T K6c - docs/wrapperPlan.md.
WHERE     GroupActions.rtn, runAction's bind (`ruleArg->groupBody =
          result->groupBody`) and whatever mints the node the callee reads.
          RuleStuff layout, beside parentLabel.
GUESS     NONE OUTSTANDING -- the mechanism is FILMED, not guessed, and the
          ordering is MEASURED: MINT -> BIND -> READ (SEQ 102 Part 1, R19). Both
          pre-registered candidates died on camera. The remaining work is a
          BUILD, not a diagnosis, and its shape is ruled in the NEXT block.

⚠ CERTIFICATES MOVED TO THE SEQ 105 CHARTER, 2026-09-01. The four listed in the
NEXT block below are now that charter's to discharge, not this file's to hold.
This citizen carries the EVIDENCE; the charter carries the acceptance.

⚠⚠⚠ FOURTH ASKING, 2026-09-01, SEQ 106. FAILED -- SAME SIGNATURE -- AND THE
FRAME SLOT WAS NEVER WRITTEN. STOP CLAUSE INVOKED: filmed, banked, reported, and
NO identity chase across the three roads.

The frame bind was BUILT and LANDED (C8): RuleStuff gained `frameArg` beside
parentLabel, groups.ext synced, full bare tokall, canary 326. Certificates 2 and
3 discharged. Then the asking, under gNoUnwrap = 1, with the camera in:

    BODY  asSubject  node=0x103077340  groupBody=0x103075be0
    CARRIERCAM rStuff field=asTake rStuff=0x0
    CARRIERCAM bind   field=asTake ruleArg=0x103074780 ruleArgBody=0x103075870
                      result=asSubject resultNode=0x10307be00
                      resultBody=0x103075be0 slot=0x0 gNoUnwrap=1
    CARRIERCAM after  ruleArgBody=0x103075be0   (want == resultBody)
    BODY  argument    node=0x10307b440  groupBody=0x103075870

THE BIND STILL WORKS. ruleArgBody went 0x103075870 -> 0x103075be0, exactly
resultBody. THE CALLEE STILL READS A DIFFERENT NODE -- 0x10307b440, carrying
0x103075870, which is ruleArg's body BEFORE the bind. Unchanged from the third
asking. The frame bind did not move this line at all.

⚠⚠ AND THE REASON IS ONE MEASURED FACT: AN INTERPRETED ACTION HAS NO rStuff, SO
A SLOT ON RuleStuff CANNOT EXIST ON THE ACTION ROAD.

    rStuff = 0x0 on 16 of 16 action binds in kant8T
    slot   = 0x0 at every bind

`if (field->rStuff) field->rStuff->frameArg = result;` never fires. That is not
a defect in the wiring -- it is Ruling D working exactly as written: rStuff
presence IS the liveness test, and an action is not a rule, so it lawfully has
none.

⚠ SO CERTIFICATES 2 AND 3 PASSED ENTIRELY ON THE SAVE REORDER, NOT ON THE SLOT.
That is worth stating plainly rather than letting two green rows imply the slot
works: the trample fix is real, measured, and landed, and it is a DIFFERENT fix
that happened to ride the same stroke. THE SLOT IS INERT CODE TODAY.

WHAT THIS DOES NOT SAY. It does not say the frame is the wrong channel. It says
RuleStuff is the wrong RESIDENCE for it on the road that binds arguments. The
charter put the slot beside parentLabel because parentLabel is the working
precedent -- but parentLabel's customers are RULES, which have rStuff by
construction, and an action is not one.

⚠⚠⚠ FIFTH ASKING NOT REACHED -- THE COPY BIND WAS BUILT AND FAILED ITS OWN
CONTROL, 2026-09-01, SEQ 107. Filmed, banked, reverted. Revert boundary C10.

THE MEASUREMENT THE CHARTER ORDERED FIRST CAME BACK ARM 1, cleanly. Censused
with PARENT ATTRIBUTION across 23 actions: every action holds exactly ONE
original `argument` (parent = the action) and AT MOST ONE copy (parent = a Token
in the cached BlocK). NOT one per mention -- ntF says `argument` twice and has no
copy; asTake says it twice and has one. The copy appears exactly when the body
passes `argument` BARE TO ANOTHER CALL (asTake's showBody(argument), k1's
k1(argument)) and never for a property read like argument.taG.

⚠ ATTRIBUTION WAS LOAD-BEARING. Without parents the same walk reported 1, 2 or 3
distinct nodes and a count that CHANGED BETWEEN INVOCATIONS of one action -- the
extras were OTHER actions' arguments reached through cross-links (`parent=k6A`
turning up in k6B's census). H11 exactly: that census would have reported a fork
that does not exist.

SO THE COPY WAS BOUND, as arm 1 directs. AND IT BROKE CERTIFICATE 2:

    K2x row 1 (DIRECT recursion)   k2xBig    still correct
    K6c       (MUTUAL recursion)   k6small   REGRESSED, was k6big

⚠ THE REASON IS THE HALF OF ARM 1 THAT DOES NOT HOLD: "one line PLUS THE EXISTING
SAVE/RESTORE". THE COPY IS NOT IN THE EXISTING SAVE/RESTORE. saveLocalFields
walks the action's DIRECT CHILDREN, and the copy lives down inside the cached
BlocK under a Token. So binding it hands the body a node that nothing brackets,
and a nested activation tramples it -- which is the very defect the save reorder
had just fixed for the original, re-introduced one level down on the node the
body actually reads. k6Barg is the case: its body passes `argument` bare, so it
HAS a copy, and K6c reads through it.

⚠ AND A COVERAGE HOLE THE FAILURE EXPOSED, now closed: K6c WAS NOT A PINNED FLEET
ROW. The fleet stayed at 101 green through the regression and said nothing; it
was caught only because the certificate was re-run by hand. K6c is now pinned in
pop.sh beside K2x row 1, its direct-recursion sibling.

WHAT IS NOT OWED: a second attempt. Bracketing the copy is a materially larger
change to machinery already under a deletion sentence (docs/jit.md section 0),
and choosing that is a ruling, not a measurement.

    THE STANDING SCORE: the SAVE REORDER is real, landed, and certified --
    K2x row 1, K6c and nestT all green on it. The frameArg slot was unreachable
    and is stripped. The copy bind is measured, reverted, and NOT recommended in
    the shape arm 1 describes.

-------------------------------------------------------------------------------

THE BIND WORKS. THE CALLEE READS A DIFFERENT NODE.

WHAT YOU JUST RAN. Under the LEGACY layout this prints two bodies that differ,
which is expected and is not the finding. The finding was taken under the FLIP
(gNoUnwrap = 1) on 2026-09-02, the acceptance line's THIRD asking, and it is the
first of the three that failed INFORMATIVELY.

THE ACCEPTANCE CRITERION, unchanged since it was ruled:

    parser(Start) receives Start.

incant/acceptStartT states it by pointer: LEGACY means the argument is a CARRIER
whose groupBody differs from the caller's; FLIPPED means the argument IS the
caller's storage, groupBody identical. Under the flip it read:

    BODY  asSubject  node=0x102fbb340  groupBody=0x102fbacd0
    BODY  argument   node=0x102fbf440  groupBody=0x102fba960

Different. A carrier, for the third time.

THE CAMERA, AND IT IS THE WHOLE VALUE OF THIS ASKING

A probe at the bind site (GroupActions.rtn, the `ruleArg->groupBody =
result->groupBody` line) filmed the bind itself:

    CARRIERCAM bind  field=caTake  ruleArg=0x100651780 ruleArgBody=0x100652960
                     result=caSubject resultNode=0x100657f00 resultBody=0x100652cd0
                     gNoUnwrap=1
    CARRIERCAM after ruleArgBody=0x100652cd0   (want == resultBody)
    BODY  argument   node=0x100657540  groupBody=0x100652960

⚠⚠ READ THOSE FOUR LINES IN ORDER, BECAUSE THEY EXONERATE THE OBVIOUS SUSPECT.
THE BIND WORKED. ruleArg's body went 0x100652960 -> 0x100652cd0, which is
exactly resultBody. The flip's other half does precisely what it was designed to
do, on the node it was handed.

THE CALLEE THEN READS A DIFFERENT NODE. The bind wrote node 0x100651780. The
callee's `argument` is node 0x100657540 -- and it carries 0x100652960, which is
ruleArg's body BEFORE the bind. So the read node is a copy or a sibling minted
from ruleArg ahead of the assignment, and the write never reaches it.

AND ONE UNWRAP OF THE CARRIER YIELDS NOTHING:

    ERROR unary * on argument -- it holds no group
    value bare      = argument      (bear-trap 26: no data, so the tag comes back)
    value unwrapped = 0

So it is not even a wrapper POINTING at the subject. Under legacy the else-arm
sets `ruleArg->setGroup(result)` and the node carries a group; under the flip
that arm is skipped and the body adoption lands elsewhere, so the read node ends
up with NEITHER. That is why the carrier is empty rather than merely indirect.

⚠ THIS IS roundTripT ROW 3, ONE ROAD OVER, AND THAT IS THE HEADLINE

The same mechanism has now been measured in three places on three different
days, and it is one mechanism, not three bugs:

    roundTripT   `x :. noPrinT` then `x.noPrinT` -- write and read land on
                 DIFFERENT NODES over one body
    broadcastT   an addGroup twin and its original -- two nodes, one body
    here         the bind writes node A, the callee reads node B

⚠ NEITHER OF THE TWO PRE-REGISTERED CANDIDATES SURVIVES AS STATED. The brief
named argument binding at the call boundary, and the `<-`/copy family. The
binding is EXONERATED ON CAMERA -- it is correct. The question that replaces
both is narrower and answerable: WHICH ROAD MINTS THE NODE THE CALLEE READS,
AND WHEN, RELATIVE TO THE BIND?

TWO OTHER MOTIONS FAILED TO MOVE, AND THEY POINT THE SAME WAY

  #35 six-shape  the three wrong arms stayed at 63; all six were pre-registered
                 to read 39. So a property read inside a compound condition
                 STILL under-filters under the flip.
  derefT         R1 and R2 both read 5, where R1 != R2 was pre-registered. Bare
                 `holder` still answers with the leaf, so the read path is still
                 unwrapping somewhere the flip did not reach.

Both say the same thing the camera says: THE FLIP REACHED THE BIND AND DID NOT
REACH THE READS.

WHAT DID MOVE, so the flip is not written off as inert:

  derefAllT R4   `*daWrap` went from an error naming the leaf to LEAF. The
                 explicit deref is correct under the flip.
  broadcastT     ARM1 and ARM2b fell from 1 to false -- PRE-REGISTERED TO STAY
                 AT PIN, so this is a finding in its own right. A copy stopped
                 inheriting and stopped receiving the broadcast. ARM3, the
                 anti-vacuity control, still reads 1, so the arms are not void.

THE FLOOR WAS TAKEN AFTER THE CAPTURE, NOT BEFORE. gNoUnwrap is back to 0, the
camera is out, and every pre-flip capture is byte-identical: the fleet row for
row, oneTest, jsonTest, derefAllT, sixShapeT and broadcastT. The opDebug unwrap
strip, which was ruled to RIDE with the flip, reverted with it -- no half-stroke
shipped.

⚠⚠ THE ORDERING RUN IS DONE, 2026-09-02 (SEQ 102 Part 1). MINT -> BIND -> READ.
CLAY'S PREDICTION CONFIRMED, and Part 2 proceeds on it.

    SEQ 30  MINT(aCTionDefinE) slotNode=0x100ced8c0 slotBody=0x100cee960 on caTake
    SEQ 32  BIND(runAction)    slotNode=0x100ced780 slotBody=0x100cee960
                               -> resultBody=0x100ceecd0
    SEQ 33  READ(showBody)     node=0x100cf8780     body=0x100ceecd0

One shared counter across four stamped sites -- the mint in aCTionDefinE, both
binds in GroupActions, and the read in showBody -- so the order is a SEQUENCE
READ OFF ONE RUN rather than an inference from four separate ones.

⚠ AND AN EXTRA FINDING THAT STRENGTHENS THE FRAME PLAN RATHER THAN BLOCKING IT:
THE MINT AND THE BIND ARE ALREADY ON DIFFERENT NODES. Mint slotNode 0x100ced8c0,
bind slotNode 0x100ced780 -- two nodes over ONE body, 0x100cee960. So there are
at least THREE nodes in play (mint, bind, read) all deriving one body, and the
multi-node problem starts EARLIER than the bind.

That is the argument for the frame channel stated as a measurement: a fix that
chased node identity would have to chase it across three roads, and moving the
slot to the FRAME dissolves the question instead of answering it three times.

NEXT: RULED -- THE FRAME BIND, SEQ 102 Part 2. Nothing below is built yet.

The shape is parentLabel's, transplanted: the slot lives on the FRAME
(RuleStuff, alongside parentLabel, same residence); the caller evaluates every
argument expression FIRST, then writes the slot, then calls -- write-last is
load-bearing; the callee LIFTS INTO A STACK LOCAL AT ENTRY, first line, before
any descent. No save/restore: the slot is a HANDOFF WINDOW, not storage. Body
adoption at the bind retires, so the write CARRIERCAM filmed stops being the
mechanism.

Certificates pre-registered: the asking on camera in CARRIERCAM's format; an
A->B->A recursion fixture (K6's shape, finally with a customer) certifying the
lift discipline; an f(g(x)) nesting fixture where the inner bind must not
clobber the outer setup; and parseRule's owed lift, since the slot it reads
becomes live traffic.

WHAT WOULD GENERATE THE ANSWER, cheapest first:
  1. Print the node pointer at every site that touches `field->get("argument")`
     or mints an argument slot, in one run, and order them against CARRIERCAM's
     two lines. The answer is a sequence, not a hypothesis.
  2. If the mint is AFTER the bind, the bind has to move or the mint has to
     adopt; if BEFORE, the mint is copying a node the bind is about to change,
     which is the write-then-copy shape roundTripT already named.
  3. The generalisation is worth pricing either way: THREE MEASUREMENTS ON THREE
     DAYS SAY A NAMED READ DOES NOT REACH A WRITTEN NODE. A fix aimed only at
     the argument road leaves the other two standing.

⚠⚠ ADDED 2026-09-02 (SEQ 116): `<-` IS CARRIER-STABLE, AND IT IS THE FIRST NAMED
READ MEASURED TO REACH A STABLE FIELD.

Everything above is a record of the argument road minting fresh nodes. This is
the first measurement of a road that does NOT, and it is on the read side rather
than the write side.

MEASURED, one run, by addrOf's FIELD column -- which is the right column here
precisely because it reports the carrier, and the carrier is the subject.
Pinned as pointerT rows L6a-L6d:

    a `<-` capture of a NAME, asked twice          field=#10, #10   SAME
    a `<-` capture of a SUBSCRIPT, asked twice     field=#6,  #6    SAME
    a DEFINE-BLOCK field, asked five times         #1 #3 #8 #9 #11  FRESH EVERY ASK

⚠ THE CONTROL IS THE THIRD ROW AND IT IS NOT OPTIONAL. A numbering scheme that
never advanced would satisfy "the field repeats" without meaning anything; the
define-block field advancing five times in the same run is what makes the two
repeats real identities rather than a stalled counter.

⚠ WHY IT BELONGS IN THIS FILE: the whole citizen is about a named read not
reaching a written node. `<-` is a named read that demonstrably reaches the SAME
field twice running. It is therefore a candidate ROAD for what step 4 is about to
build by hand -- offered as a candidate and NOT as a recommendation, because
what is measured is that the capture is stable, and NOT that it is stable across
the boundary the argument channel has to cross. Those are different claims and
only the first one has been run.

⚠ AND THE SAME MEASUREMENT EXPLAINS A CONTROL THAT HAS BEEN FAILING UNNOTICED.
roundTripT's ARM 0 probes one field twice with nothing between and declares that
the two node addresses MUST match or every address below is void. They do not
match, and have not since the fixture was born on 2026-08-31 -- because
probeNode takes its subject through the carrier, which mints a fresh field per
ask. The fixture was not in the fleet, so nothing reported it. It is in the fleet
now, with ARM 0 PINNED AT THE MISMATCH so that when this citizen discharges the
row goes red and has to be re-pinned to MATCH.
⚠ The voiding is narrow and the narrowness is the useful part: it voids the
probeNode ADDRESS lines only. ARM A, B1 and B2 read their flag through a bare
mention and never through probeNode, so the mechanism table those arms establish
is untouched by it.

⚠ VOCABULARY NOTE, NOT A REWRITE. The `NEXT: RULED` block above is written in
"frame", which SEQ 114 retired in favour of FIELD and COPY. It is left exactly as
ruled: it is a dated ruling and rewriting its words would falsify the record. Read
its "frame" as the argument channel. New text in this file uses the current
vocabulary.

⚠⚠ ADDED 2026-09-02 (SEQ 117): THE ACCEPTANCE-LINE FAILURE, MEASURED BY addrOf
FOR THE FIRST TIME -- AND IT IS WORSE UNDER THE FLIP THAN WITHOUT IT.

This arrived as the CONTROL for step 4 rather than as its result, which is why it
is here rather than in a step-4 write-up. `minionWork/probeStep4` asks one action
to print its own `argument` beside the source the caller passed, by addrOf and by
probeNode, in one run.

    gNoUnwrap = 0        source   tag s4Src     field #1   body #2
                         argument tag s4Src     field #3   body #2   SAME BODY
    gNoUnwrap = 1        source   tag s4Src     field #1   body #2
                         argument tag ARGUMENT  field #3   body #4   DIFFERENT BODY

⚠ READ THE TAG COLUMN AND THE BODY COLUMN TOGETHER, because either alone
understates it. Flip-OFF the callee at least ends up on the SOURCE'S BODY, so a
read through it reaches the caller's data by accident of the auto-unwrap. FLIP-ON
IT DOES NOT: the callee's argument is a different field carrying a DIFFERENT BODY
and wearing the tag `argument` instead of the source's name.

⚠ SO THE FLIP DOES NOT MERELY EXPOSE THIS DEFECT, IT DEEPENS IT, and that is a
scheduling fact rather than a diagnosis. The acceptance line does not fail at the
flip because the flip is wrong; it fails because the flip removes the accident
that was covering for the argument road.
⚠ Both instruments read through the carrier, so the FIELD and node columns report
the carrier and are not evidence here. The BODY column is carrier-proof and is
what carries the finding.

⚠⚠ STEP 4 WAS BUILT, MEASURED, AND REVERTED THE SAME SESSION. STOP CLAUSE
INVOKED, per SEQ 117's instruction to stop and report rather than adjust.

The build is preserved as `minionWork/step4Sweep.patch` -- the sweep at
ruleActions.rtn:459 gated on gNoUnwrap to install a POINTER (a fresh node with
gGroup at the item) instead of a copy, for every swept item except the code body.

    READ 1   switch at 0, fleet byte-identical      CONFIRMED -- 173 rows, exact
    READ 2   switch on, argument is the field       NOT TAKEN
    READ 3   switch on, argument carries the        NOT TAKEN
             source's rStuff

⚠ READS 2 AND 3 WERE NOT TAKEN BECAUSE THE RUN NEVER REACHED THEM. With the
switch on and the patch in, the process prints four lines and stops:

    RunRulE: expected a method not UnitTests
    RunRulE: expected a method not Utilities
    RunRulE: expected a method not list
    RunRulE: expected a method not s4Run

-- the include and search machinery itself, before any fixture code runs. Exit 0,
which is bear-trap territory: a parse failure abandons the rest of the file and
still exits 0.

⚠ AND THE CONTROL SAYS IT IS THE PATCH AND NOT THE FLIP. Same flip, patch
reverted, retok, rebuild: the probe runs to its sentinel and prints the table
above. So `gNoUnwrap = 1` alone is sound and the sweep change is what breaks name
resolution globally.
⚠ THE OBVIOUS ACCOUNT IS THAT THE SWEEP IS NOT SCOPED TO "the action's own
fields" -- it runs over EVERY definition in the system, grammar and registries
included, so pointer-ising every bare mention breaks lookup everywhere. THAT IS
A CANDIDATE AND IT IS NOT DIAGNOSED. It is written down as the first thing to
test, not as the answer, per the standing split.

RESTORED TO THE C22 BOUNDARY AND VERIFIED: ruleActions.rtn and jitContext.h back
to their committed blobs, retok bare, rebuilt, fleet byte-identical to C22 at 169
green / 3 red, canary 326, every other instrument green.
