Start();
include(utilities);
search reset stack Grokking Utilities list;
register(FixPar);
define
    fpWindow x=0 y=0 width=100 height=100
        fpInside x=10 y=10 width=20 height=20;
    fpProbe argument code={
        fixParentHere = 0;
        cerr "ANCHOR fixParentHere -- the A/B is on screen; row 4 is the whole issue":;
        cerr "  THROUGH AN ACTION-ARGUMENT HOLDER":;
        cerr "    1 .taG        = " argument.taG "   want fpInside   property":;
        cerr "    2 .texT       = " argument.texT "   want fpInside   property":;
        cerr "    3 .listLengtH = " argument.listLengtH "   want 4          property":;
        fpP <- argument.parenT;
        cerr "    4 .parenT     = " fpP.taG "   want fpWindow   NODE":;
        };
    fpDirect code={
        fpKid <- fpWindow["fpInside"];
        cerr "  DIRECT, NO HOLDER IN THE WAY":;
        cerr "    1 .taG        = " fpKid.taG "   want fpInside   property":;
        cerr "    3 .listLengtH = " fpKid.listLengtH "   want 4          property":;
        fpU <- fpKid.parenT;
        cerr "    4 .parenT     = " fpU.taG "   want fpWindow   NODE":;
        };
    fpScope argument code={
        cerr "  AND THE CONSEQUENCE, in displayIfVisible's own shape:":;
        :parent x y width height;
        cerr "    lookin `parent`   x = " x "  width = " width "   <- TAG ECHO, no data":;
        :argument x y width height;
        cerr "    lookin `argument` x = " x " width = " width "        <- correct":;
        };
    fpRun code={
        fpDirect();
        fpChild <- fpWindow["fpInside"];
        fpProbe(fpChild);
        fpScope(fpChild);
        cerr "FIXIT SENTINEL -- parentUnreachable reached its foot":;
        stop();

LANE:  parser
BLAST: OVERLAPS

WHY THIS BLAST STAMP -- routing is auditable, not asserted.
PRE-ROUTED BY RULING: rides the recon. Its step-2 measurement reads opDot's
implied-receiver block, a surveyed site. ⚠ AND THE SAME BLOCK WAS INDICTED INDEPENDENTLY
THIS SESSION: Instruct.rtn:305-307 is lastREF's consumer, and the opPlusPlus arms
attribution showed the ++ advance is its unnamed producer.

        };
    ;
fpRun();

AN ACTION CANNOT REACH ITS ARGUMENT'S PARENT -- AND THE ACCESSOR IS NOT
MISSING. IT IS THE HOLDER ROAD THAT LOSES IT.

REWRITTEN 2026-08-29 AGAINST THE TREE. This file previously said the gap was
one navigation direction out of six and that opDot had no parent case. THAT
WAS WRONG AND THE TREE SAYS SO. `parenT` is opDot case 2, registered in
incant/setup's GroupFields as `parenT=2`, and it returns the real parent node
-- `if !target.parent product = null; else product = target.parent`. Its old
wrapper behaviour was repaired on 2026-08-24; incant/fixits/parentStamp's own
prose records that, and minionWork/probeCanonTopo's "returns a WRAPPER" note
predates the repair and is stale. Nothing here is a language addition any more.

WHAT IS ACTUALLY BROKEN, measured 2026-08-29, one run, both roads in the same
process, with three property accessors as the control:

    accessor        kind        direct        through the holder
    .taG            property    fpInside      fpInside            ok
    .texT           property    --            fpInside            ok
    .listLengtH     property    4             4                   ok
    .parenT         NODE        fpWindow      argument            BROKEN

READ THE COLUMNS, NOT THE ROWS. Direct is correct for everything, so the
accessor works. Through the holder, every PROPERTY-returning accessor is
correct and the one NODE-returning accessor is not -- it hands back the
HOLDER, whose tag is `argument`. So this is not a missing direction. It is a
single-unwrap asymmetry between opDot cases that only shows on the road an
action's argument arrives by.

WHY THE HOLDER EXISTS. runAction binds an action's argument by
`ruleArg.group = argument` -- the action's `argument` is a FIELD POINTING AT
the subject, not the subject. Property cases read through that; the node case
does not. Adjacent to the two-faces question and to bear-trap 35's single
unwrap; contact noted, not chased.

WHAT THIS COSTS TODAY. incant/utilities' displayIfVisible opens with

    :parent x y width height;

which is meant to hoist the CONTAINER's box. `parent` is not a spelling of
anything, so aCTionScopeXP finds nothing, clears x/y/width/height, and under
bear-trap 26 the no-data locals echo their own tags -- so the arithmetic two
lines later reports

    ERROR Operator + failed on py and ph

naming an operator while the defect is two roads away. The correct spelling
today is a capture taken before the read, never a chained read off the holder.

WHAT WAS ALREADY FIXED AND IS NOT THIS ISSUE. displayIfVisible carried four
defects; three are mended: four semicolon-less `if`s (the SemI- ruling),
`:x y width height;` where lookin was `x` and is now `:argument ...`, and a
tok-spelled `.tag` that is now `.taG`.

ONE MORE THING SEEN AND NOT CHASED. `fpWindow.parenT` on the outermost node
returns a node tagged `Token`, where case 2's own text says a parentless
target yields null. A define-block member legitimately has a parent, so this
may be correct and merely surprising. It is recorded because it was seen, not
because it is graded.

A THIRD THING, CAPTURED SEPARATELY. Even with the parent road fixed,
displayIfVisible's `if goodToGo;` tests a field that HOLDS ZERO, and
incant/andProbe already ruled a bare field test goes by PRESENCE, not value --
so it reads TRUE either way. It will need `if goodToGo == 1;`.

⚠⚠ VERDICT 2026-09-01 (SEQ 103 Part 1, Tony). TWO HALVES, DIFFERENT FATES.

(a) DISCHARGED, TODAY. Tony's words: "`:parent x y width height;` in
displayIfVisible is wrong -- parent is not a field; the groupField is parenT.
Retire the line." DONE: the line is gone from incant/utilities and the comment
block above displayIfVisible now carries why, what is missing in its place, and
two spellings not to substitute for it.
⚠ NOTHING LIVE MOVED, and that was measured rather than assumed: displayIfVisible
is LATENT -- defined in incant/utilities and called from NOWHERE in the tree
(re-measured 2026-09-01; agrees with docs/andOrRung.md and incant/andProbe).
Fleet before and after: 88 green / 1 parked / 3 expected-red, byte-identical.

(b) RETEST-POST-FLIP, AND NO PRE-FLIP FIX IS TO BE BUILT. The measured defect --
.parenT through an action-argument holder returning the HOLDER -- is the flip's
territory. Tony ruled it there.

GRADE UNCHANGED -- OPEN below, and it stays OPEN because half (b) is still
exactly that. The census in the OPEN block (every node-returning opDot case,
direct and through a holder, paired) is STILL THE RIGHT NEXT MEASUREMENT and is
still Clod-sized -- it is now simply scheduled AFTER the flip rather than before
it, because the flip may change its answer.

⚠ DO NOT READ (a) AS PROGRESS ON (b). Retiring the line removed a CONSUMER of
the defect, not the defect. The accessor is still wrong through the holder --
this citizen's own row 4 still prints `argument` where it wants `fpWindow`, and
it printed exactly that today after the edit.

NEXT: OPEN

THE OLD BEST GUESS IS FALSIFIED AS WRITTEN AND IS WITHDRAWN. Candidate A was
"add a parent accessor to opDot, one more case beside 401-405". The accessor
is already there, as case 2, and it is correct on a direct capture. Building A
would have added a second parent road beside a working one and left the actual
defect untouched -- which is why the tree gets checked before a guess gets
built.

WHAT WOULD GENERATE AN ANSWER, and it is one fixture, no build:

  1  WIDEN THE CENSUS TO EVERY NODE-RETURNING opDot CASE. Two are known --
     parenT (2) and registrY (3) -- and the navigation family at 401 to 405
     (nextInParent, priorInParent, firstInList, lastInList, nextMember) all
     return nodes too. Run each one twice, direct and through a holder, in
     one process. If every node case fails through the holder and every
     property case survives it, the defect is ONE unwrap decision in opDot
     and the fix is one place. If the node cases disagree with each other,
     it is per-case and the census names which.
     ⚠ PAIR EVERY ROW WITH ITS DIRECT SIBLING. A row that fails on both
     roads is not this defect -- registrY reads wrong DIRECT as well, so it
     is either a different bug or legitimately null here, and a
     holder-only census could not tell those apart.

  2  THEN READ opDot's ENTRY, once the census says how wide it is. The
     `if !argument if lastREF { argument = target; target = lastREF.group; }`
     block is where a road can pick up or lose an unwrap, and it is guarded
     on `!argument`, so the two roads may not both reach it.

WHAT IS NOT OWED. No ruling from Tony is owed at this grade, and that is a
change from the previous version, which asked for one. The question is now a
measurement, not a decision, and the measurement is Clod-sized.
