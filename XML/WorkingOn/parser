/*******************************************************************************
	Offline Status Report
*******************************************************************************/
Read the bible for orientation and todo I pasted in. Do not respond yet. We will get to it; nothing has changed wrt to its task list and the work priorities baked in there, however, let us do a wee big of language design work first.
I did some reorganizing of the incant twk files, moving actions defined in Commands.rtn that are not really command oriented to GroupActions.rtn. I went thru those files to keep the actions in them and in Instruct.rtn in alphabetical order.
I did do some thinking on changing incant method signature for operators to change their target parameter to pass by reference instead of pass by value. You were concerned with the blast radius of that change. Still would like to discuss it with you and maybe set Tonto on a recon to assess the blast radius. It is important for op methods that return target because any changes made to target get lost as a result of pass by value. We have tripped over this in our generator work and will proabably trip over it again. By my count, Clod can verify, there are 7 op methods that return target in Instruct.rtn; 2 methods in Commands.rtn; and 11 in GroupActions.rtn.
To verify that changing the signature of gMethod, declared in GroupBody and aliased to method, I added a test to groups.twk that shows making target passed by reference instead of pass by value works. I am pasting in groups.twk for your review. Note: the test runs and then incant carries on like it was never run POP that the test did no damage. We can strip it from groups.twk after our discussion and no way to know the test ever happened.
Once we are done w/our method signature change discussion we need to decide how to proceed. Finishing up work on generating is currently at top of our task list, but if we can devise a way to make the change to the incant method signature (keeping in mind the blast radius is all encompassing) it should fix a problem that keeps poking its way into our generating work.

/*******************************************************************************
	Tests to be written
*******************************************************************************/
    assert action a la groovy to be defined and tested.
    An implied field test to show how lastDEF works in a dot op expression; and in a FOR loop; and how to explicitly set the implied field source using the @field unaryOP.
        This may imply changing how fields are found. In aCTionNamE(): if locate does not find a field, and lastREF is set, it should look in lastREF for the field (it does not at this point). Also, lastREF is scoped within a for loop and gets reset to what it was when the for loop exits. Worth demonstrating that in the test.
    A buffer test showing how setMark and unMark interact;
        also demonstrate stdout to a buffer. Take a simple action that prints to stdout then use printTO to divert print to print to a buffer;
        when printTO diverts print to a buffer, presumably that diversion applies to any subsequent prints even across different following actions until printTO runs again and undiverts. Something to illustrate by example.
    A bin used as a switch test a la generator/runGenerator
    Need incant commands to insert a field before (change GroupItem insertGroup to insertInto) or after another field

/*******************************************************************************
	Follow up clean up
*******************************************************************************/
 
