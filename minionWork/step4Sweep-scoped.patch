--- /tmp/claude-501/-Users-anthony-Library-CloudStorage-Dropbox-data-InProcess-Groups/25bdbda3-a2b2-407a-abda-d221dbfbd3ba/scratchpad/ruleActions.rtn.C23	2026-09-01 17:40:52
+++ /tmp/claude-501/-Users-anthony-Library-CloudStorage-Dropbox-data-InProcess-Groups/25bdbda3-a2b2-407a-abda-d221dbfbd3ba/scratchpad/ruleActions.rtn.s4b.clean	2026-09-01 17:42:43
@@ -425,6 +425,14 @@
             Process Attributes.
         ***********************************************************************/
 use item
+        /*  STEP 4 predicate, established BEFORE the sweep because CodE is not known
+            until partway through it: a definition CARRIES CODE. Flip-gated, so it is
+            0 in every ordinary build and the sweep below is unchanged.   ruleActions.stepFourSweep  */
+        -%
+        int stepFourCoded = 0;
+        if ( gNoUnwrap && Attributes && Attributes->get("code") )
+            stepFourCoded = 1;
+        %-
         if Attributes
             while item = Attributes.next(item)
                 if noPrint && immediateACTION {
@@ -456,7 +464,23 @@
                                 item.parentStuff = grup.rStuff;
                                 item.parentLabel = grup.parentLabel; }
                             item.rStuff.rule    = item; }
-                    grup    = NewGroup +% item; }
+                    /*  STEP 4: an ATTRIBUTE of a CODED definition is installed as a POINTER,
+                        not as a copy. Members are swept elsewhere and are untouched; a
+                        definition with no code body takes the else arm, which is the
+                        line that was always here.   ruleActions.stepFourSweep  */
+                    -%
+                    if ( stepFourCoded && item != CodE )
+                        {
+                        GroupItem *stepFourPtr = new GroupItem(item->groupBody->tag);
+                        stepFourPtr->groupBody->gGroup              = item;
+                        stepFourPtr->groupBody->flags.data          = 6;
+                        stepFourPtr->groupBody->flags.isInitialized = 1;
+                        grup = NewGroup->addAttribute(stepFourPtr);
+                        }
+                    else
+                        grup = NewGroup->addAttribute(item);
+                    %-
+                    }
         /***********************************************************************
             If there is code NewGroup is flagged as coded. The code gets processed
             by processCode() the first time NewGroup is included in an expression.
