Start();
include(unitTests);
include(utilities);
search reset stack Grokking UnitTests Utilities list;
register(P32b);
define
    p32Ok=0;
    p32N=0;

    p32Run code={
        cerr "=== probe32b CONTROL -- single-statement if with else ===":;
        p32Ok = 1;
        if p32Ok == 1;      p32N = 7;
        else                p32N = 9;
        cerr "p32N " p32N:;
        cerr "PROBE32b SENTINEL -- reached the foot":;
        stop();
        };
    ;
p32Run();
