Start();
include(unitTests);
include(utilities);
search reset stack Grokking UnitTests Utilities list;
register(P32c);
define
    p32Ok=0;
    p32N=0;

    p32Run code={
        cerr "=== probe32c THE COMBINATION -- multi-statement indented arm THEN else ===":;
        p32Ok = 1;
        if p32Ok == 1;
            p32N = 7;
            cerr "then-arm ran, p32N " p32N:;
        else
            p32N = 9;
        cerr "p32N " p32N:;
        cerr "PROBE32c SENTINEL -- reached the foot":;
        stop();
        };
    ;
p32Run();
